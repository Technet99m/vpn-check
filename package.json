{
  "name": "vpn-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --target=bun-windows-x64 --outfile dist/vpn-check.exe",
    "build:current": "bun build src/index.ts --compile --outfile dist/vpn-check"
  },
  "devDependencies": {
    "@types/node": "^25.2.3"
  }
}
